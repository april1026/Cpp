#include <iostream>
using namespace std;
int main() {
    int n, k;
    cin >> n >> k;
    int input[n];
    int key[n];
    for (int i = 0; i < n; ++i) {
        cin >> input[i];
    }
    for (int i = 0; i < n; i++) {
        cin >> key[i];
    }
    
    int yet[n];
    for (int i = 0; i < n; i++) {
        yet[i] = key[i] - input[i];
        if ()
    }

    cout << output;

    return 0;
}
